{
  "name": "kitacatat",
  "private": true,
  "version": "0.1.0",
  "description": "Personal finance tracker for family use: Telegram bot (Go) + React dashboard",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
